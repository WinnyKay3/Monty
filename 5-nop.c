#include "monty.h"

/**
 * nop - This does just nothing
 * @stack: does not matter anyway
 * @line_number: nothing
 *
 * Return: empty
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
