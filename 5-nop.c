#include "monty.h"

/**
 * nop - This does just nothing
 * @stack: does not matter anyway
 * @line_cnt: nothing
 *
 * Return: empty
 */
void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
